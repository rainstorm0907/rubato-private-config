---
name: maple-gear-session
description: "메이플 장비 컨설팅 전용 세션 — ~/dev/maple 워크스페이스, 티어 응답 프로토콜, Nexon API·다이제스트·consult 조합"
metadata: 
  node_type: memory
  type: project
  originSessionId: 6d71a1d5-297b-4ae1-82fa-569c6b4a688c
---

2026-07-05 구축: 기존 코덱스 메이플 스레드(568MB, 답변 5~10분)가 느렸던 원인 = ① 웹페이지 원문(JS번들 포함)을 컨텍스트에 통삼킴 ② 지식이 파일이 아닌 대화 기억에만 존재 ③ 매 질문 제로부터 재리서치. 해결로 **`~/dev/maple` 전용 워크스페이스** 구축 — 새 코덱스 스레드는 이 폴더에서 열기.

**구조:** `AGENTS.md`(티어 프로토콜: T0 즉답=KB+스냅샷, T1 정찰=latest_digest, T2 심층=consult 백그라운드) · `kb/`(sources·gear-goals·patch-log — 파일이 기억, 우진님 언급 출처는 sources.md에 즉시 추가) · `scripts/fetch_character.sh`(Nexon Open API 장비 스냅샷+diff, **.env의 NEXON_API_KEY 필요 — 기존 세션엔 키 없었음, 우진님 발급 대기**) · `scripts/latest_digest.sh`(아카 메이플채널·디시 lith·인벤·공식공지를 숨김 크롬 new-tab→text→tab-close로 정찰, 소스당 ~45항목).

**주력 소스(기존 세션 빈도 기반):** arca.live/b/maplestory(750회) > 디시 lith(리스항구 갤) > 인벤 > 공식 > maplescouter(환산). **캐릭터 3개(2026-07-05 API 확인):** 레공레=제일 메인(챌린저스2, Lv.280 레테) · 레공(루나, Lv.282 호영) · 오렌지솥밥(루나, Lv.282 렌, 주보 목표·예산 20억). API 키는 test_ 등급(.env, 초당 제한 빡빡 — 스크립트에 sleep+재시도 반영).

**유튜브:** 지명(메인)·맑음·이득충 — kb/youtube-channels.txt 등록, RSS 다이제스트 검증 완료. **영상 핵심 추출:** timedtext 자막 API는 PoT 토큰 게이트로 비로그인 접근 차단(2024+, yt-dlp도 동일 벽) → **Gemini 영상 분석이 정식 루트**(agbrowse --vendor gemini, 숨김 크롬에 Google 로그인 1회 필요 — 2026-07-05 현재 로그인 대기). 자막 있는 영상은 scripts/yt_transcript.sh가 제목·트랙 인식까지만 동작.

**agbrowse 요령:** 자동화 수집·폴링은 **quiet-browse**(탭 활성화 없는 CDP)로 — agbrowse new-tab/select-tab/text는 창을 작업 위로 올림(digest는 이미 quiet-browse로 전환됨, 소스당 ~45항목 무교란 검증). `web-ai poll`은 "resolver mismatch" 버그 → `quiet-browse text chatgpt.com`으로 회수(생성 중엔 "검색 중" 문구). **Gemini = rainstorm0907@gmail.com 로그인 완료(2026-07-05)** — 영상 분석 루트 준비됨, 단 web-ai query는 탭을 활성화하므로 실제 영상 분석 첫 테스트는 미실시. KB 시드 완료: kb/meta-2026-07.md(2026-03-19 스타캐치 삭제 개편·17/18/22성 기준·3카5앜 로드맵·경매장 API 부재→시세 UNVERIFIED 정책·test키 일1,000건 제한).

**환산 계산식 방법론 확립 (2026-07-06, 클로드):** 코덱스 세션(019f3037)이 만든 스펙업 예측 계산식을 검토·보강. 산출물: `kb/calc-framework-review.md`(이론 SSOT), `kb/prediction-log.md`(예측→실측 누적), `kb/README.md`(확장 구조), AGENTS.md "스펙업 예측 계산" 섹션.
- **환산주스탯 = 모든 딜스탯을 주스탯 1종으로 환산한 단일지표(김수호 철학, Maplescouter 표준).** 2층: 스탯→환산주스탯(선형계수)→최종뎀(비선형 체감). 템환(장비)/헥환(장비+6차)/쌀피엠(직업별 헥사효율).
- **코덱스 오진 규명:** 헥사 예측+4.2% vs 실측+8.03% 과소예측을 "선형확장 오차"라 진단했으나 방향 반대(선형확장이면 과대여야). 진짜는 **입력추정**(떡작 결과 저평가). → 오차 4분류(계수/곱연산/입력추정/측정조건).
- **예측 프로토콜:** 전(스냅샷)→예측(Maplescouter 추가스펙 시뮬 1순위, 없으면 효율계수 곱연산+3구간)→후(재측정)→대조(4분류)→prediction-log 누적.
- **Maplescouter 실측(레공레):** 환산380 38,071/헥환 30,516/기운66·조각1129. **레테 정축여축 버그로 API 전투력 저평가 → Maplescouter가 SSOT.** 조작은 claude-in-chrome(본 크롬 새 탭, WAN 탭 안 건드림). 검색창에 캐릭명 → /ko/info. 시뮬레이터는 페이지 하단(스크롤 리셋 잦음).
- **헥환<환산은 정상(우진님 확인):** 헥사(6차)를 충분히 찍어야 헥환이 환산을 넘음 — 레공레는 헥환30,516<환산38,071이라 **아직 HEXA 투자 전, 최우선 투자처가 HEXA라는 신호**. "얼마나 찍어야 넘느냐"는 직업 쌀피엠 함수.
- **헥사 스펙업 계산 방법론 (2026-07-06 우진님 지정):** 솔 에르다 **기운은 사냥으로 천천히 차니 병목 아님**. **조각이 진짜 부족한 자원(병목).** 그래서 헥사 업글은 반드시 **"조각 N개 투입 → 헥환 얼마↑ → 최종뎀 몇%"**로 계산하고, 다른 스펙업(예: 보조무기 메소)과 비교할 땐 **조각 효율(최종뎀%/조각) vs 메소 효율(최종뎀%/억)**로 맞대어 본다. 현재 레공레(게임 실측 2026-07-06): **기운 0개(솔에르다, 422/1000 진행 — 1000당 1개)/조각 303**. ⚠️ Maplescouter 헥사페이지의 "66/1,494·1,129/41,743"은 **누적 투자 진행률**이지 보유량 아님(착각 주의). 기운0이라 헥사스탯 **신규 개방 불가**(개방은 기운 소모), 단 이미 개방된 헥사스탯 떡작·스킬코어 강화는 조각으로 가능. 조각값 환산 = **550만/조각**(우진님 지정, 사이트 기본 700만 아님).
- **스펙업 비교 표준 (우진님 지정 2026-07-06): Maplescouter가 제공하는 최종뎀·억당효율을 기준으로 쓴다** — 보조/장비 교체/헥사 전부 이 억당효율로 맞대 비교. 손계산 KMS 공식은 보조 수단.
- **HEXA는 사이트 "헥사코어 순서"(조각 효율순) 그대로 진행.** 스킬 코어 강화(레테=임펠·임프린트·인보크 템플러/바르가르)와 헥사스탯 떡작을 사이트가 섞어 우선순위 매김 → 위에서부터. 헥사스탯 떡작 1회 ≈ 324조각→+8%(prediction-log 실측)는 참고 앵커. 조각이 병목, 기운은 사냥으로 자연 축적이라 무시.
- **⭐ 헥사 순서 SSOT = Maplescouter `/ko/hexa?name=레공레` (2026-07-07 확립):** 상단 "분석&최적화" 드롭다운 → "헥사코어 순서". 컨트롤: 효율기준=조각·시작기준=현강화·조각값(우진님 550만)·"본인 스펙 추천구간 보기" 체크. **순서 그리드는 React prop `hexaOrder`(154스텝)에 통째로 있음** — 페이지는 CSR(`__NEXT_DATA__` 없음), 셀 요소의 `__reactFiber`에서 상위로 타고 올라가 `memoizedProps.hexaOrder` 추출. 각 요소=배열 `[코어명, 도달레벨, 이미지, 기운, 조각, 누적기운, 누적조각, v7=100억당최종뎀근사, v8, coreId]`, **필드[6]=누적조각(검증됨)**, 인덱스=효율 내림차순 추천순서. DOM outerHTML/속성 덤프는 이미지URL 때문에 `[BLOCKED]` → 값 필드만 골라 반환. 그리드 좌→우·위→아래 = hexaOrder 인덱스 순, 셀 우하단 숫자=도달레벨, hover 툴팁=코어명/100억당%/조각·기운/헥환증가.
- **레공레 조각303 실행계획 (2026-07-07 실측):** 조각303 커버 = **순서1,2뿐 = 임펠 VI/팩트 매니페스트(마스터리코어) 4→6레벨, 합 48조각**(사이트가 이 2셀만 보라색 하이라이트로 확인). 순서3=임프린트 →14레벨은 조각290(누적338)+기운누적15 → 조각 ~35개+기운 더 모아야 진입(여기선 기운이 새 병목). 순서4=헥사스탯2 떡작=조각524(누적862).
- **⭐ 조각-메소 "별도 지갑" 프레임 (우진님 2026-07-07 확정, 기존 통합환산 대체):** 조각은 헥사 전용 자원이고 사냥으로 자연축적·다른 소비처 없음 → **보유 조각은 억당환산 무관하게 사이트 순서대로 소비(매몰비용)**. 메소는 보조/장비에 억당효율로 **독립 판단**. 헥사 vs 보조는 택일 아니라 **병행**. → 731-prediction의 "보조 후순위(조각-메소 통합 억당비교라서)" 결론은 **수정**. ⚠️ 발견: 550만 기준 억당효율 **보조 W1 21.3%/100억 > 헥사 최상위(순서1) 15.9%/100억**. 그리고 **prediction-log 실측(떡작 0.451%/억)과 사이트 헥사효율(0.159%/억) 3배 괴리 → 떡작 실측 입력추정 오류 의심(검증 대기)**.
- **레테 데미지 공식 정밀계산 baseline (2026-07-06 Maplescouter 직접입력 실측):** 총마력=2142base×(1+마력%48)=3170 · INT: base5036/INT%328/flat20409 → 총INT≈41,963 · LUK base2314/86%/flat90 · 데미지%71 · 보스데미지%339 · 최종데미지162.66 · 방무97.5 · 크뎀75/크확100. **보스 최종뎀 곱연산 모델**: StatTerm(총INT×4+총LUK) × 총마력 × 뎀증(1+데미지%+보스뎀%) × 최종뎀 × 크리 × 방무. 보조 잠재 비교 시 변하는 항(마력%/보공(보스뎀)%/데미지%/주스탯%)만 계산하고 나머지(크리·방무·최종뎀)는 상쇄. 데미지%와 보공%는 같은 뎀증 라인(합연산).
- **다음 세션 과제:** 레테 쌀피엠 순위(헥사vs아이템 비중), INT100 vs 800 체감 시뮬 실측(우진님 협조 예정). 해방/챌섭 로드맵은 코덱스 branchpoints 3개(full-growth-flow 등)에 근거와 함께 정리돼 있어 검증 완료.

**경매장 웹 시세 도구 (2026-07-06, 게임체인저):** `auction.maplestory.nexon.com/buy?searchTab=condition`를 **claude-in-chrome**(넥슨 로그인+게임 접속된 본 크롬)으로 조회. 게임접속 탐지 = 레공레 코디 렌더 여부. 검증(2026-07-06): 골든클로버벨트13억/가엔링8억/트왈마7.5억 모두 시세 일치=적정가. **시세 UNVERIFIED 정책 → 실조회로 대체**(AGENTS 규칙2).

**⭐ 최적 도구 = 내부 API 직접 호출 (2026-07-06 확립, get_page_text 대체):** get_page_text는 **옵션 실값(마력18/보공30)을 안 줌** → 필터 카운트로만 추정하다 "유니크 보조 보공 없음" 오진. 해법: `javascript_tool`로 `POST https://api.mskr.nexon.com/v1/market/web/items/searches/tool-tip`(credentials:include) 직접 fetch → 매물별 옵션 JSON. 응답 `items[].toolTip.upgradeInfo.potential.entries=[{grade,text}]`(grade 1레어2에픽3유니크4레전, text="마력 +9%"), `additionalPotential`=에디, `attackPowerDiff`=전투력증가량, `pricePerItem`=개당가. body: worldId48/accountId83039204/characterId3545121(레공레)/limit≤20/potentialGrade 1~4/filters.keyword. **제약: limit≤20이어야 페이지네이션 됨(50은 1페이지만) · 새 페이지 첫 요청 빈배열 잦음→재시도 · search 쿼터 100/일(daily-limit 엔드포인트, 난사금지·캐시).** 방법전환: 옵션 실값 받으니 optionRows 서버필터(합산모호·인코딩함정) 불필요 → keyword+grade로 받아 **JS로 entries 파싱해 조합·컷·꿀매물 직접 계산**. 미스터리 규명: 유니크 녹스깃펜 104건 중 싼50건 전부 보공0=시장 희소(필터버그 아님). 부위별×등급별 잠재값 SSOT = **kb/potential-values.md**(ChatGPT Pro 리서치, 컷은 "첫줄+낮은등급 추가줄"). 상세 kb/auction-web-howto.md.

관련: [[consult-chatgpt-research]] [[codex-meight-global-setup]] [[wan22-colab]]
