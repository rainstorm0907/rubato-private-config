---
name: hama-deploy-topology
description: Hama 프로젝트의 실제 라이브 배포 구조 — EC2/Vercel/챗봇/SSH 복구 (repo 문서와 다른 운영 실태)
metadata: 
  node_type: memory
  type: project
  originSessionId: 74df4385-e728-40b9-be6c-40146c7a5ffa
---

Hama(중고 가격비교) 라이브 배포 실태 (2026-06-11 저녁 갱신, repo 문서보다 최신·정확):

- **프론트**: Vercel 프로젝트 `hama-main-frontend` (계정 `rainstorm0907`), prod alias `https://kdt-hama.vercel.app`. 재배포 = `cd code/frontend/Hama && npx vercel --prod --yes`. `.vercel/project.json`에 연결됨.
- **백엔드 (2026-06-11 팀원 dlwnsgh1130이 Spring 배포하며 구조 변경)**: EC2(시드니 ap-southeast-2, instance `i-0f3d433aadc4e131d`)에 nginx(:80) + FastAPI(:8000, systemd `hama-fastapi`) + Spring Boot(:8001, systemd `hama-backend`, jar `/home/ubuntu/main/code/backend/build/libs/hama-backend-0.0.1-SNAPSHOT.jar`). nginx 라우팅: `/api/products/`·`/api/health` → :8000(FastAPI), 그 외 `/api/` → :8001(Spring, auth/찜/알림/마이페이지). `vercel.json` rewrite는 포트 없이 `http://15.134.191.154/api/:path*` (= nginx 80). 8001은 보안그룹에서 외부 차단(내부 nginx 경유만), 8000은 외부 오픈 상태.
- **DB 일원화 완료 (2026-06-11 저녁)**: FastAPI·Spring 모두 우진님 계정 Supabase `kgpkcvuawbpgdcyykfdk`(Hama, ap-northeast-2) 사용. user 도메인 테이블 10개를 Spring 엔티티 모양(bigint PK+시퀀스, password 컬럼, Y/N 플래그)으로 재구성함 — repo `code/supabase/migrations/20260611120000_align_user_domain_tables_to_spring_schema.sql` 참고. Spring 접속은 pooler `aws-1-ap-northeast-2.pooler.supabase.com:6543` (자격증명 EC2 `/etc/hama-spring.env`, 전환 전 팀원 DB 설정 백업본 `/etc/hama-spring.env.bak-teammate-db`). 팀원 계정 Supabase `emiqzwdbsqegednmlqoa`는 이제 미사용(폴백용으로 데이터 그대로 둠). DB 비밀번호에 `@` 포함 → URL 통째 파싱 깨지니 user/password 분리 전달 필수. repo의 `docs/supabase_schema.sql`(uuid/auth.users 설계)은 폐기된 설계 — 실제는 `docs/db_schema.sql` 계열.
- **회원 탈퇴는 soft delete** (`account_status='WITHDRAWN'`, 행 유지) → 탈퇴 계정이 닉네임/이메일 중복 체크를 영구 점유하는 이슈 있음 (팀 공유함).
- **Elastic IP `15.134.191.154` 고정됨**(2026-06-11, eipalloc-0ab943e34da1abf9b, 인스턴스에 연결). 이제 stop/start 해도 IP 안 바뀜. vercel.json도 이 IP. (이전엔 EIP가 없어서 IP가 바뀌며 사이트 다운 사고 발생함 → EIP로 해결.)
- **챗봇 라이브 = Spring 정교본 (2026-06-11 심야 전환, 로그인 필수)**: nginx `/api/chatbot/` → **:8001(Spring)** 로 변경(백업 `/home/ubuntu/nginx-hama.bak-chatbot-*`). 비로그인 401 = 의도된 동작. Spring 챗봇 = 템플릿→가격조언→Gemini 의도분석→FAQ→OpenSearch 추천(items 카드)+chat_history 저장 (33파일 2,454줄). 프로드 검증: 템플릿/추천(items 10개+가격필터 파싱)/history 전부 OK. jar는 EC2 트리 그대로 클린 재빌드 성공(이번엔 `/opt/gradle/gradle-8.14.3/bin/gradle clean bootJar`로 EC2에서 빌드 됨, 동일 사이즈 재현 — RAM 여유에 따라 실패할 수도 있으니 그땐 로컬 빌드 경로 사용). FastAPI 경량본(`gemini_chatbot.py`)은 :8000에 남아있으나 nginx 미경유. Gemini 키: Spring `/etc/hama-spring.env`, FastAPI `python/.env` — **`AQ.A…` 53자도 `?key=`로 정상**(403 leaked/400 expired/429는 키 상태 문제). insights는 여전히 FastAPI(:8000)에만 있음.
- **chat_faq 시드 완료 (2026-06-11)**: 준호님 CSV(`~/Downloads/chatbot_expected_answers.csv`, 32그룹)를 `|`패턴 explode → **188행 삽입** (테이블 컬럼은 question_pattern/answer_text 2개뿐; CSV의 intent/priority는 저장 안 됨). ⚠️ **FAQ 분기 거의 안 탐**: `GeminiClientService.applyFallbackValues`가 기본 intent를 PRODUCT_RECOMMEND로 깔아 ChatbotService의 FAQ 분기(`FAQ`/`UNKNOWN`일 때만)에 도달 못함 — 고치려면 준호님 분류 로직 수정+재빌드(조율 필요).
- **관리자 기능 진짜로 작동함 (2026-06-11 밤 완성)**: `User.role` `@Transient`→`@Column(name="ROLE")` 전환 + users.role 컬럼 추가(main 커밋 `0b7ca88`) + jar 재빌드·교체. **관리자 계정 `hama@naver.com` / `123123`** (DB에서 role='ADMIN', `/api/mypage/admin/check`→`{"admin":true}` 검증됨). 프론트(main)는 `/admin` 라우트가 admin/check 결과로 가드됨. 새 관리자 지정 = DB에서 `UPDATE users SET role='ADMIN' WHERE email=...`.
- **Spring jar 빌드 방법**: EC2(RAM 2G, 여유 ~100Mi)에선 빌드 불가. repo에 gradle-wrapper.jar 없음 → 로컬 맥에서 `/tmp/gradle-9.4.1/bin/gradle bootJar` + `JAVA_HOME=~/Library/Java/JavaVirtualMachines/ms-21.0.10/...` (toolchain 21). ⚠️ **EC2 `/home/ubuntu/main`은 팀원 fork(dlwnsgh1130) + 커밋 안 된 핫픽스 트리** — 라이브 jar의 소스는 shortKDT/main이 아니라 이 트리임. 재빌드 시 EC2 트리를 rsync로 내려서 빌드할 것(쉘 이력 참고). jar 백업 `/home/ubuntu/hama-backend.jar.bak-role`.
- **vercel.json 라우팅 분기 해소됨 (2026-06-11 밤, 근본 해결 적용)**: EC2 라이브 nginx를 수정해 `/api/products/`(prefix, insights 포함)·`/api/health` → :8000(FastAPI), `/api/chatbot/` → :8001(Spring, 심야 전환됨), 나머지 `/api/` → :8001(Spring)로 정리함 (백업 `/home/ubuntu/nginx-hama.bak-*`). 이제 **vercel.json은 전체를 nginx(:80)로 보내는 main 버전이 정답** — 옛 분리 버전(auth만 nginx) 쓰면 mypage/찜/알림이 FastAPI로 가서 깨짐. repo `nginx-hama.conf.example`도 동기화(커밋 70e94f7). 프로드 종단 검증: search·insights·chatbot·login·찜·알림 전부 200.
- **프로드 = main 커밋 9cef9ac 빌드 (2026-06-11 밤)**: naver 제거 + 팀원 찜/알림·챗봇 고도화(fork 머지 3c0c92e) + 가격비교/insights + **관리자 페이지 실데이터**. ⚠️ 다른 세션 폴더(HamaMain_opensearch·HamaMain)에서 `vercel --prod` 치면 옛 브랜치 기반이라 또 롤백 사고 남 — **배포는 항상 main 기준 worktree에서** (상시 worktree `/Users/wooojin/HamaMainLive`, .vercel 링크 복사돼 있음). 프로드 FE = main `26d5d08` 빌드(2026-06-12: 관리자 리디자인+이상데이터 정렬+시세 placeholder 필터+차트 날짜 기간 필터+마이페이지 탭 캐시. PR #60 머지 포함).
- **repo = 라이브 동기화 완료 (2026-06-12 오후, main `4f4c4c5`)**: EC2에만 있던 챗봇 추천 가격순 정렬(RecommendationService.orderChatbotRecommendations)·nginx/systemd 라이브 설정·catch-up 마이그레이션 3개·백엔드 통합 README를 main에 수록. docs도 정비됨 — `docs/db_schema.sql`(라이브 PostgreSQL DDL 정리본), `docs/db_erd.md`(mermaid ERD, 21테이블), `docs/requirements.md`(실구현 기준 요구사항정의서; 기존 코드 가이드는 `docs/code_guidelines.md`로 이동), supabase_schema.sql은 폐기 헤더. **HamaMain_opensearch 폴더도 main 추적으로 전환됨**(옛 codex-opensearch-integration 잔재는 stash + /tmp tar 백업; 로컬 FastAPI는 .env 있음 — `cd code/backend/src/main/python && .venv/bin/uvicorn api_server:app --port 8000`, OpenSearch 미설정이라 python 폴백으로 동작). EC2/Vercel 재배포 불필요했음(코드 동일).
- **관리자 페이지 실데이터 완성 (2026-06-11 밤, main 9cef9ac)**: 이상데이터 = 적재 없이 items 쿼리 — FastAPI `GET /api/products/anomalies?mode=low_confidence|accessory&limit&offset` + `/summary`(저신뢰<0.5 2,763 / 악세서리 420). 악세서리 토큰 = `python/config/accessory_tokens.csv`(블랙리스트 패턴, 15토큰). 유저 조회 = Spring `GET /api/admin/users`(admin 403 가드, 찜 수 JOIN). 프론트 AdminPage 탭 2종+페이지네이션+유저 검색/필터. `/admin` 직접 진입 튕김 버그 수정(isAuthChecked 게이트). FastAPI 라우트 등록 순서 주의: anomalies가 `{platform}/{pid}` 앞이어야 함.
- **⚠️ EC2 FastAPI 3차 회귀 → 복구 완료 (2026-06-12 새벽)**: 팀원이 06-11 23:21 KST에 fork 새 커밋 `d4cf826 최종이어라`로 EC2 트리 갱신+재시작 → insights·anomalies 라우트 또 소실(상세 차트 '현재 상품' 폴백, 관리자 이상데이터 500/404). **EC2 python은 이제 lib/ 레이아웃**(flat 아님, fork가 구조 변경; sys.path 픽스는 fork에 흡수됨). 복구 = main의 ① lib/supabase_repository.py 끝에 클러스터 인사이트+이상데이터 블록 append(+`from collections import defaultdict`) ② api_server.py import 확장 + anomalies/summary/insights 라우트를 `{platform}/{pid}` 데코레이터 **앞에** 삽입. 패치 스크립트 EC2 `/tmp/ec2-patch/`, 백업 `*.bak-20260611-154439`. 검증: insights(갤럭시 버즈 클러스터 6개)·anomalies(2763/420)·search 전부 OK. **SSH 차단 원인 = 집 IP 변동**: SG(sg-0c6234b044f47d17f)의 22번은 IP 화이트리스트 방식이라 IP 바뀌면 또 막힘 — 현재 119.203.102.118/32 등록됨(2026-06-12). 막히면 `curl checkip.amazonaws.com`으로 IP 확인 후 우진님에게 SG 추가 요청.
- **검색 정크 필터 체계 (2026-06-12 새벽, main 11a4696~77aa5fe)**: '아이폰 최저가 1원'·'아이폰16프로 평균 390만' 원인 = quality_flags 구멍. 수정: ① `invalid_price` = price<1000(`MIN_PLAUSIBLE_PRICE`) **또는 price≥2천만(`MAX_PLAUSIBLE_PRICE`, 999,999,999원류 구매/교환 placeholder — 5백만~2천만엔 맥스튜디오 등 실매물 있어 상한 2천만)** 또는 교환글+10만미만 또는 **교환글+1천만이상(`EXCHANGE_PLACEHOLDER_CEILING`)** ② NOISE_TOKENS += 구매합니다/구합니다/구해요/구함/사요/사기꾼/조심 ③ 시세 요약(최저/평균)은 **중앙값×0.2**(`SUMMARY_PRICE_FLOOR_RATIO`, 0.03→0.2로 상향: 본체 검색에 케이스·공박스(중앙값 2~5%)가 최저가로 새던 것 차단) 미만 제외 — OS 경로는 median 집계+하한 미달시에만 2차 집계(repository.refine_summary_below_floor), python 폴백은 search_summary 미러(상한도 적용). 전부 documents.py 상수가 SSOT. **플래그 변경 후엔 재sync 필수**: `cd code/backend && src/main/python/.venv/bin/python opensearch/sync_from_supabase.py` (sync 스크립트도 sys.path.insert 섀도잉 픽스 적용됨 89dc499; 인덱스 45,249 풀싱크. bulk 후 refresh가 read timeout=2로 가끔 죽지만 인덱싱은 완료된 것). 검증: 아이폰16프로 최저 35,000→230,000원·평균 3,909,326→1,042,311원. 잔존: 검색 리스트(낮은가격순) 상위에 토큰 미적중 잡글 소수(2,450원 '아이폰 16프로' 등 — floor는 요약 카드에만 적용, 리스트는 그대로), 맥북 6건 매치(커버리지).
- **차트 기간 필터 = 날짜 기반 (2026-06-12 새벽, main 77aa5fe)**: 상세 모달 3달/1달/1주 버튼이 '마지막 N개 점'(take 32/8) 방식이라 점 6개뿐인 데이터에서 전부 동일하게 보이던 문제 → BE points에 `date`(ISO) 필드 추가(supabase_repository의 price_history/similar_group_price_history/_daily_points 3곳) + FE createRangePoints를 **마지막 데이터 날짜 기준 N일 윈도우**로 전환(date 없는 점은 통과=폴백 안전). 비교 차트(PriceCompareChart) x축은 점≤12개면 모든 일자 라벨 표시(이전엔 짝수 인덱스+마지막 규칙이라 4점→'05.27/06.10' 2개만 보여 "이틀치뿐"으로 오해됨). **근본 한계 = price_history가 클러스터 단위론 듬성**(예: 아이폰 16 = 05.27+06.08~10 4일치, 5/28~6/6 크롤링 공백) — 매물 대부분이 6/8~10 신규 크롤이라 과거 이력 자체가 없음. 발표 시 "수집 시작 후 누적되는 구조"로 설명할 것.
- **⚠️ fork pull = 패치 소실 4차 위험 (PR #60으로 부분 해소)**: 팀원이 fork에 upstream 머지 후 PR #60(d4cf826+dd129a3+280c91e: 챗봇 추천 platform/pid·답변검증·유사매물 가격이력·검색 의도필터)을 올렸고 main에 머지함(7ab034a) — fork 작업은 이제 main에 수록됨. 단 **fork는 main의 최신 커밋(정크필터 aab07b9·ffe0cae 등)보다 뒤처져 있어** EC2에서 git pull하면 여전히 미커밋 수술 패치(insights/anomalies/median floor)와 충돌·소실 위험. EC2 재구성할 땐 main을 새로 받는 게 정답. PR의 알려진 흠: 폰 검색 page≥2에서 `expanded_search_limit`가 from 오프셋을 4배로 키워 페이지네이션이 어긋남(라이브에서도 동일, P3 공유사항).
- **가격비교 모달 찜/알림 구멍 수정 (2026-06-12, main d1ea53e 배포)**: PriceCompareWorkspace가 ProductDetailModal에 isLoggedIn/onLoginRequired를 안 넘겨 가격비교 경로에서 찜/알림이 조용히 무시되던 버그 — AppRoot→PriceCompareModal→Workspace, MyPage→MyPagePriceCompareTab→Workspace 양쪽 prop 연결로 해결. PriceInsightChart에 isClustersLoading 추가(insights 조회 중 '시세 분류 확인 중…' 표시). 찜/알림 API 자체는 정상 검증됨(login→add→list→alert→delete 전부 200, Supabase 저장 확인).
- **⚠️ EC2 사고 이력 (2026-06-11 밤, 재발 주의)**: 다른 작업자가 EC2 python을 fork 버전으로 통째 교체 → ① flat 레이아웃이 lib/ 레이아웃으로 바뀌며 EC2 전용 패치(insights·챗봇)가 소실, ② **opensearch-protobufs(pip, opensearch-py 3.x 의존성)의 최상위 `opensearch` 패키지가 로컬 `code/backend/opensearch` 패키지를 가려** FastAPI crash loop(검색 502). 해결 = api_server.py에서 `sys.path.insert(0, BACKEND_DIR)` (main에 수록됨). ③ EC2 트리 재빌드로 User.java role fix도 한 번 회귀했음. **EC2 repo는 fork(dlwnsgh1130)를 추적**하므로 팀원이 shortKDT/main을 fork에 머지하기 전까지 EC2에서 git pull하면 main 작업이 또 날아감 — 팀 공유 필수.
- **서버 python 모듈은 FLAT 레이아웃** (`supabase_repository.py`, `opensearch_repository.py`가 루트). 로컬은 `lib/` 패키지 구조라 **api_server.py를 통째로 덮으면 import 깨짐** — 수술적 패치만 할 것.
- **SSH 복구**: 원본 키 `hama-backend-key`(.pem) 분실. 복구키 `~/.ssh/hama-backend-recover`(ed25519)를 EC2 Instance Connect로 `ubuntu` authorized_keys에 심어 복구함. 접속: `ssh -i ~/.ssh/hama-backend-recover ubuntu@<EC2_IP>`. 락아웃 시 보안그룹 22번을 Instance Connect CIDR(시드니 `13.239.158.0/29`)에 열고 Instance Connect로 키 재주입.
- **AWS 작업**: 로컬에 aws cli/자격증명 없음 → EIP/SG 등은 AWS 콘솔 **CloudShell**(프롬프트 `cloudshell-user@`)에서 실행. EC2 박스 터미널(`ubuntu@ip-172-31-`)엔 aws 권한 없음.

관련: [[hama-project]] (있으면)
