interface:
  display_name: "Outpost"
  short_description: "Send a packet to a web-strong ChatGPT project agent"
  default_prompt: "Use $outpost with exactly one --quality xhigh or --quality pro flag on `outpost send`; write a self-contained packet, never switch to another sender automatically, and verify the returned answer locally."
policy:
  allow_implicit_invocation: true
